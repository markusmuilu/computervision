# Install PyTorch first, matching your GPU: https://pytorch.org/get-started/locally/
# Tested with torch 2.11 (CUDA 12.8) and 2.14 (CPU).
rfdetr==1.3.0
supervision==0.26.1
transformers<5  # rfdetr 1.3.0 does not import under transformers 5
opencv-python
pillow
tqdm
